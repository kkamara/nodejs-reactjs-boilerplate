extends layouts/main.pug

block scripts
  //- script(src='/jquery.js')
  //- script(src='/pets.js')

block content
  h1= title
  ul
    each item in data
      li
        span= item.uid+', '+item.author+', '+item.title
